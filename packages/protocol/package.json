{
  "name": "@tessera/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Genesis records, member identity and signed wire records. The root of trust for the mesh.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node dist/selftest.js"
  },
  "dependencies": {
    "@tessera/sim": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/node": "22.7.5"
  }
}
